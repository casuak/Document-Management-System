<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.abc.ssm.modules.tools.dao.DatabaseDao">
    <select id="showTables" resultType="string">
        show tables
    </select>

    <select id="selectColumnsInTable" parameterType="string" resultType="tableColumn">
        select COLUMN_NAME    as name,
               COLUMN_COMMENT as comment,
               DATA_TYPE      as type
        from INFORMATION_SCHEMA.Columns
        where table_name = #{tableName}
          and table_schema = 'docManager'
    </select>

    <insert id="insert" parameterType="map">
        insert into ${tableName}
        <foreach collection="columnList" separator="," item="column" index="index" open="(" close=")">
            ${column}
        </foreach>
        values
        <foreach collection="data" separator="," item="row">
            <foreach collection="row" open="(" close=")" separator="," item="cell">
                #{cell}
            </foreach>
        </foreach>
    </insert>

    <select id="select2ColumnInTable" parameterType="tableColumn" resultType="map">
        select ${fkOriginalField}, ${fkReplaceField} from ${fkTable}
    </select>
</mapper>